workspace_dir = "/zeroclaw-data/workspace"
config_path = "/zeroclaw-data/.zeroclaw/config.toml"
# This is the Ollama Base URL, not a secret key
api_key = "http://host.docker.internal:11434"
default_provider = "ollama"
default_model = "llama3.2"
default_temperature = 0.7

[gateway]
port = 42617
host = "127.0.0.1"
allow_public_bind = false

[mcp]
enabled = true

[[mcp.servers]]
name = "webdav"
transport = "stdio"
command = "/usr/local/bin/webdav-mcp"
args = []
# WEBDAV_URL, WEBDAV_USER, WEBDAV_PASS are inherited from the container environment.
# Set them via docker-compose.override.yml or .env.
